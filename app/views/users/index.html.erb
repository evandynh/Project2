<h1>Users</h1>
<h2>Hello <%= current_user.name if current_user%> </h2>

<%= link_to 'Log Out', logout_path, method: :delete %>

<ul>
  <% @users.each do |user| %>
  <li> <%= user.name %> </li>
  <% end %>
</ul>
